// Copy this file to app_secrets.dart and fill in your values.
// app_secrets.dart is gitignored — never commit it.
class AppSecrets {
  AppSecrets._();

  // AdMob ad unit IDs — https://admob.google.com
  // Create a Native Ad unit in your AdMob account and paste the IDs here.
  static const String admobAdUnitAndroid =
      'ca-app-pub-XXXXXXXXXXXXXXXX/XXXXXXXXXX';
  static const String admobAdUnitIos =
      'ca-app-pub-XXXXXXXXXXXXXXXX/XXXXXXXXXX';

  // CollectAPI key — https://collectapi.com (optional, used for Brent oil price)
  // 'apikey demo' works as a free/limited tier key.
  static const String collectApiKey = 'apikey demo';
}
